from distutils.core import setup

setup(
        name = 'fvar',
        version = '0.1',
        description = 'Import and export custom-formatted files',
        author = 'collodi',
        license = 'MIT',
        packages = ['fvar']
)
